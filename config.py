# ============================================================
#  ПАРАМЕТРЫ ПОИСКА  —  меняешь только этот файл
# ============================================================

# Районы Otodom (слаги в URL). Проверь/поправь при первом запуске.
OTODOM_DISTRICTS = ["wola", "zoliborz", "srodmiescie"]

# Районы OLX (человекочитаемо; используются как фильтр по тексту локации).
OLX_DISTRICTS = ["Wola", "Żoliborz", "Śródmieście"]

# Жёсткий фильтр по районам для ОБОИХ сайтов: объявление проходит,
# только если его локация содержит один из этих районов.
# Отсекает «районы вразнобой». Оставь пустым списком [], чтобы выключить.
TARGET_DISTRICTS = ["Wola", "Żoliborz", "Śródmieście"]

# --- Основные фильтры ---
PRICE_MAX = 6000          # потолок аренды, PLN (без учёта czynsz/чинша)
AREA_MIN = 50             # минимальная площадь, м²
ROOMS_MIN = 2             # минимум комнат (1-комнатные отсекаются)

# Только собственники?  True = bez pośredników,  False = агентства тоже.
# Это и есть тот «переключатель», который ты хотел.
OWNERS_ONLY = True

# --- Поведение скрапера ---
# Пауза между запросами к сайтам, сек (не наглеть, чтоб не забанили).
REQUEST_DELAY = 4
# User-Agent как у обычного браузера.
USER_AGENT = (
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
    "(KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36"
)
# Сколько первых карточек с каждой страницы разбирать (свежие идут первыми).
MAX_ITEMS_PER_PAGE = 40
